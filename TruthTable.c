#include<stdio.h>
// negation not ~ 
// conjunction and & 
// disjunction or + 
// implication if...then... -> 
// two-way implication if and only if <-> 
int main(){
	char s[] = {"(~p->q)+(r->q)->((p<->r)->q)"};
	
	printf("hello");
	return 0;
}
